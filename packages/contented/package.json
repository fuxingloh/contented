{
  "name": "@contentedjs/contented",
  "version": "0.0.0",
  "private": false,
  "repository": {
    "url": "git+https://github.com/levaintech/contented"
  },
  "type": "module",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "bin": {
    "contented": "./cli.js"
  },
  "files": [
    "dist",
    ".preview",
    "cli.js"
  ],
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json && node build-preview.js",
    "clean": "rm -rf dist",
    "lint": "eslint src"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown"
    ],
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "eslintConfig": {
    "parserOptions": {
      "project": "./tsconfig.json"
    },
    "extends": [
      "@stickyjs"
    ]
  },
  "dependencies": {
    "@contentedjs/contented-processor": "0.0.0",
    "@parcel/watcher": "^2.2.0",
    "clipanion": "3.2.1",
    "debounce": "^1.2.1",
    "ignore-walk": "^6.0.3",
    "typescript": "4.9.5"
  },
  "devDependencies": {
    "@contentedjs/jest-preset": "0.0.0",
    "@types/debounce": "^1.2.2",
    "@types/ignore-walk": "^4.0.0"
  }
}
